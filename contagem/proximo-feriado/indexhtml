<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quanto falta para o Próximo Feriado? | Data e Hora</title>
    <style>
        :root { 
            --bg: #0f172a; --card: #1e293b; --text: #f8fafc; 
            --accent: #fb7185; --secondary: #94a3b8; --border: rgba(148, 163, 184, 0.1);
        }
        [data-theme="light"] {
            --bg: #f1f5f9; --card: #ffffff; --text: #0f172a; 
            --accent: #e11d48; --secondary: #64748b; --border: rgba(0, 0, 0, 0.05);
        }

        body { 
            background: var(--bg); color: var(--text); font-family: system-ui, sans-serif; 
            margin: 0; padding: 20px; display: flex; flex-direction: column; align-items: center; 
            justify-content: center; min-height: 100vh; text-align: center;
        }

        .nav-links { position: absolute; top: 20px; display: flex; gap: 10px; }
        .btn-nav {
            padding: 8px 18px; border-radius: 50px; background: var(--card);
            color: var(--text); text-decoration: none; font-size: 0.85rem;
            border: 1px solid var(--border); transition: 0.2s;
        }

        .container { max-width: 600px; width: 100%; }
        
        .label-top { text-transform: uppercase; letter-spacing: 2px; color: var(--secondary); font-size: 0.9rem; margin-bottom: 10px; }
        h1 { font-size: 2.5rem; margin: 0 0 20px 0; color: var(--accent); }
        
        /* Contador */
        .countdown-grid {
            display: grid; grid-template-columns: repeat(4, 1fr); gap: 15px; margin: 40px 0;
        }
        .time-unit {
            background: var(--card); padding: 20px 10px; border-radius: 16px;
            border: 1px solid var(--border); box-shadow: 0 10px 20px rgba(0,0,0,0.1);
        }
        .number { display: block; font-size: 2.5rem; font-weight: 800; color: var(--text); }
        .unit { font-size: 0.75rem; text-transform: uppercase; color: var(--secondary); font-weight: 600; }

        .holiday-info {
            background: rgba(251, 113, 133, 0.1); padding: 15px 25px;
            border-radius: 50px; display: inline-block; font-weight: 600; color: var(--accent);
        }

        @media (max-width: 500px) {
            h1 { font-size: 1.8rem; }
            .number { font-size: 1.8rem; }
        }
    </style>
</head>
<body data-theme="dark">

    <div class="nav-links">
        <a href="/" class="btn-nav">🏠 Início</a>
        <a href="../" class="btn-nav">⏱️ Voltar</a>
    </div>

    <div class="container">
        <div class="label-top">Contagem Regressiva</div>
        <h1 id="holiday-name">Carregando...</h1>
        
        <div id="holiday-date-str" style="color: var(--secondary); margin-bottom: 10px;"></div>

        <div class="countdown-grid">
            <div class="time-unit">
                <span class="number" id="days">00</span>
                <span class="unit">Dias</span>
            </div>
            <div class="time-unit">
                <span class="number" id="hours">00</span>
                <span class="unit">Horas</span>
            </div>
            <div class="time-unit">
                <span class="number" id="minutes">00</span>
                <span class="unit">Minutos</span>
            </div>
            <div class="time-unit">
                <span class="number" id="seconds">00</span>
                <span class="unit">Segundos</span>
            </div>
        </div>

        <div class="holiday-info" id="holiday-status">Buscando feriado mais próximo</div>
    </div>

    <script>
        const feriados2026 = [
            { name: "Confraternização Universal", date: "2026-01-01" },
            { name: "Segunda-feira de Carnaval", date: "2026-02-16" },
            { name: "Terça-feira de Carnaval", date: "2026-02-17" },
            { name: "Sexta-feira Santa", date: "2026-04-03" },
            { name: "Tiradentes", date: "2026-04-21" },
            { name: "Dia do Trabalho", date: "2026-05-01" },
            { name: "Corpus Christi", date: "2026-06-04" },
            { name: "Independência do Brasil", date: "2026-09-07" },
            { name: "Nossa Sra. Aparecida", date: "2026-10-12" },
            { name: "Finados", date: "2026-11-02" },
            { name: "Proclamação da República", date: "2026-11-15" },
            { name: "Dia da Consciência Negra", date: "2026-11-20" },
            { name: "Natal", date: "2026-12-25" },
            { name: "Ano Novo", date: "2027-01-01" }
        ];

        function getNextHoliday() {
            const now = new Date();
            return feriados2026.find(h => new Date(h.date + "T00:00:00") > now);
        }

        const target = getNextHoliday();
        const targetDate = new Date(target.date + "T00:00:00").getTime();

        document.getElementById('holiday-name').textContent = target.name;
        document.getElementById('holiday-date-str').textContent = new Date(target.date + "T00:00:00").toLocaleDateString('pt-BR', { day: '2-digit', month: 'long', year: 'numeric' });

        function updateCountdown() {
            const now = new Date().getTime();
            const distance = targetDate - now;

            const d = Math.floor(distance / (1000 * 60 * 60 * 24));
            const h = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            const m = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
            const s = Math.floor((distance % (1000 * 60)) / 1000);

            document.getElementById('days').textContent = d.toString().padStart(2, '0');
            document.getElementById('hours').textContent = h.toString().padStart(2, '0');
            document.getElementById('minutes').textContent = m.toString().padStart(2, '0');
            document.getElementById('seconds').textContent = s.toString().padStart(2, '0');

            if (distance < 0) {
                clearInterval(interval);
                location.reload(); // Recarrega para buscar o próximo feriado
            }
        }

        const interval = setInterval(updateCountdown, 1000);
        updateCountdown();

        // Tema
        const savedTheme = localStorage.getItem('theme-pref-dataehora') || 'auto';
        if (savedTheme === 'auto') {
            const hora = new Date().getHours();
            document.body.setAttribute('data-theme', (hora >= 6 && hora < 18) ? 'light' : 'dark');
        } else {
            document.body.setAttribute('data-theme', savedTheme);
        }
    </script>
</body>
</html>
